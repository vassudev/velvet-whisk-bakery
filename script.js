// Optional: subtle re-trigger or scroll effects for landing
document.addEventListener('DOMContentLoaded', function () {
  const stage = document.querySelector('.pastry-stage');
  if (stage) {
    stage.classList.add('ready');
  }
});
